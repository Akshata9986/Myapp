function ComponentA() {
    return (
        <div>
            <h1>this is ComponentA</h1>
        </div>
    );
}

export default ComponentA;//default export


//default--only one default export
//named export--n number of times we can export